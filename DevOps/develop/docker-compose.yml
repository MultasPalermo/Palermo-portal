version: "3.9"

services:
  # 🌐 Frontend alcaldiafetch - Desarrollo
  alcaldiafetch-front-develop:
    container_name: ${CONTAINER_NAME}
    build:
      context: ../..   # raíz del proyecto Angular
      dockerfile: Dockerfile
    image: ${IMAGE_NAME}:latest
    restart: always
    ports:
      - "4200:${PORT}"           # 4200 host → 80 contenedor
    env_file:
      - .env
    networks:
      - alcaldiafetch_network          # 👈 valor literal, no variable
    labels:
      - com.alcaldiafetch.environment=develop

# 🔗 Red externa compartida con backend
networks:
  alcaldiafetch_network:
    external: true
